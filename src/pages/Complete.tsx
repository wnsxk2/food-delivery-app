import React from 'react';
import {StyleSheet, View} from 'react-native';

interface CompleteProps {}

function Complete({}: CompleteProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default Complete;
