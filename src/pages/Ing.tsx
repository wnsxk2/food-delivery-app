import React from 'react';
import {StyleSheet, View} from 'react-native';

interface IngProps {}

function Ing({}: IngProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default Ing;
